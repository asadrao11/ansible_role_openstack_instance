---
OS_PROJECT_DOMAIN_NAME: "{{ os_server.openrc.domain | default('default') }}"
OS_USER_DOMAIN_NAME: "{{ os_server.openrc.domain | default('default') }}"
OS_PROJECT_NAME: "{{ os_server.openrc.project }}"
OS_USERNAME: "{{ os_server.openrc.user }}"
OS_PASSWORD: "{{ os_server.openrc.password }}"
OS_AUTH_URL: "{{ os_server.openrc.keystone_endpoint }}"
OS_IDENTITY_API_VERSION: "{{ os_server.openrc.keystone_api_ver | default('3') }}"
OS_IMAGE_API_VERSION: "{{ os_server.openrc.glance_api_ver | default('2') }}"
