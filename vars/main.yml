---
OS_PROJECT_DOMAIN_NAME: "{{ openstack_instance.openrc.domain | default('default') }}"
OS_USER_DOMAIN_NAME: "{{ openstack_instance.openrc.domain | default('default') }}"
OS_PROJECT_NAME: "{{ openstack_instance.openrc.project }}"
OS_USERNAME: "{{ openstack_instance.openrc.user }}"
OS_PASSWORD: "{{ openstack_instance.openrc.password }}"
OS_AUTH_URL: "{{ openstack_instance.openrc.keystone_endpoint }}"
OS_IDENTITY_API_VERSION: "{{ openstack_instance.openrc.keystone_api_ver | default('3') }}"
OS_IMAGE_API_VERSION: "{{ openstack_instance.openrc.glance_api_ver | default('2') }}"
